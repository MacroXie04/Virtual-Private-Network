{
  "name": "virtual-private-network",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hardened multi-user VLESS+REALITY gateway with embedded Tailscale exit-node egress.",
  "engines": {
    "node": ">=24.20.0 <25"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "start": "node src/controller-server.js",
    "bootstrap": "node src/bootstrap.js",
    "healthcheck": "node src/healthcheck.js"
  }
}
